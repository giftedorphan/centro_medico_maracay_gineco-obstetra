<nav class="navbar navbar-inverse navbar-static-top" role="navigation" id="topnavbar">
  <div class="container-fluid">
    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><%= link_to 'Registrar Paciente', new_patient_path%></li>
        <li><%= link_to 'Listar Paciente', patients_path%></li>
      </ul>
      <form class="navbar-form navbar-left" role="search">
        <%= form_tag search_path, method: :get, :class=> 'navbar-form' do %>
          <div class="form-group">
            <%= text_field_tag :search_cedula, params[:search_cedula], placeholder: 'Cedula del cliente', class: 'form-control vi_client_id_number' %>
          </div>
          <%= button_tag(type: "submit", class: "btn btn-default btn-sm vi_search_client") do %>
            <span class="glyphicon glyphicon-search" aria-hidden="true"></span>
          <% end %>
        <% end %>
      </form>
      <ul class="nav navbar-nav navbar-right">
        <% unless current_patient.nil? %>
        <ul class="nav navbar-nav navbar-right">
          <p class="navbar-text"><span>Paciente Actual: </span></p>
          <li><%= link_to current_patient.nombre, patient_path, {class: 'navbar-link'}%></li>
        </ul>
      <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>